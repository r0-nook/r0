'use strict';
const users = {
    fullName = undifine,
    firstName = "R0",
    lastName = "R1"
}
function restoreNames(users) {

  if (users.firstName === undefined) {
    users.firstName = "1";
  };
}
