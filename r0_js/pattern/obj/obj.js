// только для чтения
Object.freeze(obj);

// проверка содержимого ||
	obj[id].hasOwnProperty('prop')
   if (prop in obj[id]) {}

// создать
	Obj={'name' : "Ivan", age : 25, hiredYear : 2017}
	//в обж.кей key1 и адд вал превратив кей1 в []
		obj[id]['key1']=[value];
// добавить
Obj.name = "Ivan"
Obj['name'] = "Ivan"
	//в обж.кей.кей вал
		obj[key][key1]=value;
// удаление
delete Obj.name;
delete object[rKey];
// add metod
let Obj = {};                                  //Объявляем объект Obj
Obj.sayAge = n => {                    //Объявляем метод sayAge для объекта Obj
  console.log("Obj is " + n + " years old");  //Тело метода sayAge - вывод текста
// обращение
	//к свойствам обж из метода
	let Obj = {name : "Ivan"};
	Obj.sayName = () => console.log("My name is " + this.name);
	//к содержимому свойствам
	for(key in object) object[key]
// стработало обращение к содержимому обж.кей
for (const key in object) {
 if (typeof (object[key]) === 'number') {
   rSum += object[key];
//странно
for (const Key in obj) {
  for (const rKey of obj[Key]) {