for (let i = 0; i < array.length; i++) {
  i % 2 === 0 ? rResult[0] += array[i] : rResult[1] += array[i];
}
// найти нечётные числа от i до n
for(let i=1; i<=n; i++) i%2==1 ? /**/ :0
// найти чётные числа от i до n
for(let i=2; i<=n; i++) i%2==0 ? /**/ :0