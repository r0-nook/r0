const r2 = r0.split('');// превращение строки в масив
for (let i = 0; i < r2.length; i++) {
	if(){
		delete r2[i];
	}// удаление одного елеммента из массива
}
const r3 = r2.join(''); // превращение масива в строку

//////// удаление елеммента из массива
	for (let i = 0; i < numbers.length; i++) {
		if(){
			r0.splice(i, 1);
		}// удаление одного елеммента из массива
	}

//строку в масив без конкретных еллемментов и обратоно в сторку
	return s.split('!').join('');
	return s.replace(/!/g, '');

// можно удалить лично не выискивая ел в масс
	array.reduce((a,b) => a+b) - rMax - rMin

//удаление rElement в масиве
  for (let i = 0; i < rClon.length; i++) {
    if (rElement === rClon[i]) {
      rClon.splice(i, 1);
      break;
    }
  }
// первый и последний еллемменты массива
	rResult.shift();
	rResult.pop();