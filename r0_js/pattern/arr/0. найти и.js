//.includes
  const check = (rArr,findEl) => rArr.includes(findEl);

// for for
  for (let i = 0; i < rArr.length; i++) {
    if (findEl === rArr[i]) { return true;
    for (const rKey of rArr) {
      if (rKey === findEl) { const rFound = findEl;

// for иф .includes
  for (let i = 0; i < str.length; i++) {
    if(rVowel.includes(str[i])) {

// найти и удалить через индекс оф .indexOf()
  const rFoundIndexEl = rArr.indexOf(findEl);
    if (rFoundIndexEl !== -1) {
      rArr.splice(rFoundIndexEl, 1);
      rFound = rArr[rFoundIndexEl];
      rArr[rFoundIndexEl] = ;

// найти и удалить через фор иф
  for (let i = 0; i < rClon.length; i++) {
    if (rMax === rClon[i]) {rClon.splice(i, 1); break

// for иф includes del
  for (let i = 0; i < str.length; i++) {
    if (rVowels.includes(str[i]) || rVowelsUp.includes(str[i])) {
      delete rStr[i];
// for number
  for (let i = 0; i < l.length; i++) {
    if (typeof l[i] === 'number') {
