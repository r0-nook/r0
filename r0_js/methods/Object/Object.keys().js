// возвращает масив ключей, аналог  for...in
// разница между циклом и методом в том, что цикл перечисляет свойства
// и из цепочки прототипов
for (let name in suspectInfo) {if}
Object.keys(suspectInfo).find(
