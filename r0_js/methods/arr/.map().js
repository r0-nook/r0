// проходит заданый масив и применяет к указанным еллемментам твою функцию
let r0Arr = r1Arr.map(/*фк*/ r0Num => r0Num * 2)
// разворот мапом
var str = '12345';
[].map.call(str, function(x) {
  return x;
}).reverse().join('');
// масив строк в цифры
['1', '2', '3'].map(Number)